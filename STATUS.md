# Release status

Current status: **Candidate / source-complete**. The pipeline package, offline unit tests (12, no weights required), a local CPU smoke run in the Windows venv, and `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1) exist; this is the card pass only. No tutorial notebook ships yet (`tutorials/` is absent), so there is no clean-runtime notebook evidence and the repository is not release-grade until a `NOTEBOOK_SPEC` 1.0 notebook is added and its execution for the exact candidate commit is recorded.
