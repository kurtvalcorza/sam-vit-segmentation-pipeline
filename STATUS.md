# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package, offline unit tests (12, no weights required), a local CPU smoke run in the Windows venv, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the `TASK-INFERENCE` tutorial `tutorials/sam_vit_segmentation_colab.ipynb` (NOTEBOOK_SPEC 1.0) exist and pass static validation (`tools/validate_release_assets.py`). Unit tests and static conformance checks are expected to run in GitHub Actions. Nothing here is clean-runtime notebook evidence; the repository is not release-grade until such evidence for the exact candidate commit is recorded in `docs/release-verification.md`.
